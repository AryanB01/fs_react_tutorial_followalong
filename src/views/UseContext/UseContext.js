// Import React, and the context created in ContextProvider with useContext hook 


// Create a UseContext function
const UseContext = () => {
    // Step 1: Use custom hooks to access the current background theme and its update function.
   

    // Step 2: Define a function to set the body background based on the current theme.
  
    // Step 3: Use useEffect to set the body background when the current theme changes.
   
    return (
        <main>
            {/* Step 4: Button to toggle the theme */}
           
        </main>
    );
}

export default UseContext;
